name: PR Checks
on:
  pull_request:

permissions:
  contents: read

jobs:
  build:
    name: PR Checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/workflows/composite/build-checks
