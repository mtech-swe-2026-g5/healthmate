/**
 * Seed script placeholder.
 * Add seed data here after models and migrations exist.
 */
async function main() {
  console.log('No seed data configured.');
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
